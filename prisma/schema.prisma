// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Organization {
  id            String   @id @default(cuid())
  name          String
  planType      String   @default("professional")
  status        String   @default("active")
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  brands        Brand[]
  users         User[]
}

model User {
  id            String   @id @default(cuid())
  orgId         String
  organization  Organization @relation(fields: [orgId], references: [id])
  username      String   @unique
  displayName   String
  role          String   @default("member")
  status        String   @default("active")
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model Brand {
  id                String   @id @default(cuid())
  orgId             String
  organization      Organization @relation(fields: [orgId], references: [id])
  name              String
  alias             String?
  industry          String
  positioning       String?
  coreValues        String?
  status            String   @default("active")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  products          Product[]
  faqs              Faq[]
  evidences         Evidence[]
  terms             Term[]
  contentTasks      ContentTask[]
  questions         ObservationQuestion[]
  metrics           MetricSnapshot[]
  tickets           OptimizationTicket[]
}

model Product {
  id                String   @id @default(cuid())
  brandId           String
  brand             Brand    @relation(fields: [brandId], references: [id])
  name              String
  category          String
  params            String?  
  scenarios         String?  
  status            String   @default("active")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}

model Faq {
  id            String   @id @default(cuid())
  brandId       String
  brand         Brand    @relation(fields: [brandId], references: [id])
  question      String
  answer        String
  riskLevel     String   @default("low")
  evidenceId    String?  // 关联单个证据
  status        String   @default("active")
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model Term {
  id            String   @id @default(cuid())
  brandId       String
  brand         Brand    @relation(fields: [brandId], references: [id])
  type          String   // preferred, alias, forbidden
  value         String
  status        String   @default("active")
  createdAt     DateTime @default(now())
}

model Evidence {
  id            String   @id @default(cuid())
  brandId       String
  brand         Brand    @relation(fields: [brandId], references: [id])
  title         String
  url           String?
  sourceType    String
  authorityLevel String  @default("medium")
  status        String   @default("active")
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model ContentTask {
  id            String   @id @default(cuid())
  brandId       String
  brand         Brand    @relation(fields: [brandId], references: [id])
  type          String   
  contentType   String   
  channel       String?  // 渠道模板
  status        String   @default("pending")
  inputText     String?
  outputText    String?
  auditReport   String?  // 合规审核报告 JSON
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model ObservationQuestion {
  id            String   @id @default(cuid())
  brandId       String
  brand         Brand    @relation(fields: [brandId], references: [id])
  text          String
  category      String
  priority      String   @default("medium")
  samples       ObservationSample[]
}

model ObservationSample {
  id            String   @id @default(cuid())
  questionId    String
  question      ObservationQuestion @relation(fields: [questionId], references: [id])
  platform      String
  answerText    String
  snapshotUrl   String?
  mentionFlag   Boolean  @default(false)
  errorFlag     Boolean  @default(false)
  consistencyScore Int?   // 事实一致性评分 0-100
  createdAt     DateTime @default(now())
}

model MetricSnapshot {
  id            String   @id @default(cuid())
  brandId       String
  brand         Brand    @relation(fields: [brandId], references: [id])
  platform      String
  mentionRate   Float
  errorRate     Float
  createdAt     DateTime @default(now())
}

model OptimizationTicket {
  id            String   @id @default(cuid())
  brandId       String
  brand         Brand    @relation(fields: [brandId], references: [id])
  title         String
  suggestion    String
  status        String   @default("open")
  priority      String   @default("medium")
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model AuditLog {
  id            String   @id @default(cuid())
  orgId         String?
  userId        String?
  actionType    String   
  resourceType  String?
  resourceId    String?
  description   String?
  createdAt     DateTime @default(now())
}
